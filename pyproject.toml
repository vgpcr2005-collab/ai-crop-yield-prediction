[project]
name = "ai-crop-yield-prediction"
version = "1.0.0"
requires-python = ">=3.10"
dependencies = [
    "Flask==2.3.0",
    "Flask-CORS==4.0.0",
    "pandas==1.5.3",
    "numpy==1.24.3",
    "scikit-learn==1.2.2",
    "scipy==1.10.1",
    "Werkzeug==2.3.0"
]

[tool.vercel]
entrypoint = "backend.app:app"
